spring:
  jpa:
    show-sql: false
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

# Uncomment these when you want to hide the API docs from the public in production
# springdoc:
#   api-docs:
#     enabled: false
#   swagger-ui:
#     enabled: false